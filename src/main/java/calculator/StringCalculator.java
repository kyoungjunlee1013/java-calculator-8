package calculator;

public class StringCalculator {
    StringCalculator() {
    }

    public int add(String text) {
        // 1. 핵심 요구사항 구현: null 또는 빈 문자열 체크.
        if (text == null || text.isEmpty()) return 0; // 0을 반환한다.
    }

    // TODO: 향후 여기에 파싱, 예외처리, 덧셈 로직이 추가됩니다.

    //현재 단계에서는 임의로 0 반환되도록 처리한다.
    retrun 0;
}
